# Anthropic API key for Luna (AI coach).
# Get one at https://console.anthropic.com/settings/keys
# NOTE: In production this must move behind a server proxy — shipping the key
# in the client binary is insecure. This is for development only.
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
